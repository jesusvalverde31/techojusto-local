'use strict';
const {spawn}=require('node:child_process'),path=require('node:path'),{createApp}=require('./server.cjs');const port=4328,server=createApp({dataDir:path.join(__dirname,'data')});server.on('error',error=>{console.error(error.code==='EADDRINUSE'?`No se inició: el puerto ${port} ya está ocupado.`:error.message);process.exitCode=1;});server.listen(port,'127.0.0.1',()=>{const url=`http://127.0.0.1:${port}`;console.log(`TechoJusto está disponible en ${url}\nPulsa Ctrl+C para detener solo este proceso.`);const opener=spawn('cmd',['/c','start','',url],{detached:true,stdio:'ignore',windowsHide:true});opener.unref();});function close(){server.close(()=>process.exit(0));}process.on('SIGINT',close);process.on('SIGTERM',close);
